# 配信設定 config（あなたの事業の値）

このファイルを `config.md` にコピーして、自社の値を埋めてください。
スキルは判断の前提として、まずこの `config.md` を読みます。
**ここに入れた数値はあなたが育てる部分です。最初は仮置きで構いません。運用しながら更新してください。**

> `config.md` は自社の数値が入るので、共有リポジトリには commit しないことを推奨します（`.gitignore` 済み）。

---

## 事業・商材
- 商材：<例：B2B SaaS の無料トライアル獲得>
- 主要コンバージョン：<例：無料登録の完了>
- 手前のプロキシイベント（学習を稼ぐ用）：<例：ランディングページ到達 / フォーム開始>

## 市場・ターゲティング
- 配信する国 / 言語：<例：日本 / 日本語（コード値は API で検証して記入）>
- 年齢・性別：<例：25〜55 / 全性別>
- 初期オーディエンス方針：<例：広め（興味指定なし）から開始>

## 予算
- 日予算（初期）：<例：¥____/日>
- テスト期間と総額：<例：__週間で ¥____>
- 予算を上げる条件：<例：CPA が目標内で安定し、サンプル十分になったら段階的に +20%>

## 単価の基準線（判定に使う）
- 目標CPA：<¥____>  ← Scale 判断の基準
- 許容上限CPA（これを超え続けたら Kill）：<¥____>
- Scale 開始ライン / Kill ライン：<¥____ / ¥____>

## 入札
- 初期入札戦略：<例：最低コスト（上限なし）で学習>
- 切り替え条件：<例：学習脱出後に 目標CPA（¥____）へ>

## 学習フェーズ
- 最適化イベントの目標件数（学習完了目安）：<例：7日で __件>
- 0→1 のとき学習を稼ぐ方法：<例：初期は手前のプロキシイベントで最適化>

## 計測
- ピクセル / CAPI 接続状況：<接続済み / 未接続>
- 各コンバージョンの計測経路：<例：登録完了 = CAPI、購入 = 決済 webhook>

## 運用体制
- 日次チェック担当 / 時間：<>
- 週次レビュー曜日：<>
- 異常時の通知先：<>
